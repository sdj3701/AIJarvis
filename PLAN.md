# 자비스(Jarvis) 개인 AI 비서 개발 플랜

> 작성 목적: 로컬 PC에서 동작하는 자비스형 비서를 **단계적으로** 만들기 위한 상세 계획서  
> 기준 환경: Windows 10/11, **NVIDIA GeForce RTX 4070 Ti (VRAM 12GB)**  
> 전략: **하이브리드** — 두뇌(LLM)는 API, 손발·기억·음성은 로컬  
> 기억 저장: **D 드라이브** (`D:\Jarvis\memory`)
> 개발 진입점: [`README.md`](./README.md) · Phase별 구현서: [`docs/`](./docs/README.md)
> 문서 버전: **v0.4** — 사용자 요청에 따른 opt-in 로컬 웨이크워드 음성 경로 반영

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [목표와 비목표](#2-목표와-비목표)
3. [하드웨어·현실적 한계](#3-하드웨어현실적-한계)
4. [전체 아키텍처](#4-전체-아키텍처)
5. [저장 구조 (D 드라이브)](#5-저장-구조-d-드라이브)
6. [기술 스택](#6-기술-스택)
7. [개발이 진행되는 전체 흐름](#7-개발이-진행되는-전체-흐름)
8. [Phase별 상세 계획](#8-phase별-상세-계획)
9. [기억·피드백·성장 방식](#9-기억피드백성장-방식)
10. [정보 신뢰성 (검색·요약 검증)](#10-정보-신뢰성-검색요약-검증)
11. [안전·권한 설계](#11-안전권한-설계)
12. [테스트·데모 시나리오](#12-테스트데모-시나리오)
13. [일정·학습 로드맵 연결](#13-일정학습-로드맵-연결)
14. [리스크와 대응](#14-리스크와-대응)
15. [의사결정 체크리스트](#15-의사결정-체크리스트)
16. [바로 시작할 첫 작업](#16-바로-시작할-첫-작업)

---

## 1. 프로젝트 개요

### 1.1 한 줄 정의

텍스트(나중엔 음성)로 지시하면, **PC를 조작**하고, **웹/문서를 찾아 근거와 함께 요약**하며, 대화·교정을 **D드라이브에 쌓아** 다음에도 더 맞게 응답하는 개인용 AI 에이전트.

### 1.2 왜 하이브리드인가

| 구성 | 로컬(4070 Ti) | 클라우드 API |
|------|---------------|--------------|
| PC 조종, 파일, 자동화 | 적합 | 불필요/위험 |
| STT/TTS | 적합 | 가능하나 로컬이 유리 |
| 고품질 추론·대화 | 7B~14B는 가능, 최상위는 부족 | 적합 |
| 최신 웹 지식 | 모델 가중치만으로는 부족 | 검색 도구 + LLM이 적합 |

**결론:** “똑똑한 뇌”는 API, “귀·입·손발·기억”은 로컬이 v1에 가장 현실적이다.

### 1.3 최종 사용자 경험 (v1)

1. 채팅창/단축키로 호출한다.
2. “메모장 열어”, “이 폴더 열어” 같은 단순 조종을 한다.
3. “OO 주제 찾아서 요약해줘” → 요약 + 링크를 받는다.
4. “말투는 짧게”, “이건 틀렸고 이게 맞아” → 다음 대화에 반영된다.
5. 마이크로 말해도 같은 일을 한다.

---

## 2. 목표와 비목표

### 2.1 목표 (릴리스별)

**MVP — Phase 0~3 필수**

- API 기반 멀티턴 대화
- 대화 raw 로그 + 세션 요약 저장
- facts / corrections 기반 개인 기억
- 웹 검색 요약 + 출처 URL
- 로컬 문서 RAG (`docs\`)
- 민감정보 필터와 외부 API 전송 통제
- 비정상 종료 후 세션 복구

**v0.5 — Phase 4~6 필수**

- 화이트리스트 기반 PC 도구 실행
- 위험 동작 전 사용자 승인
- 경로·URL·도구 인자 검증과 감사 로그
- 에이전트 멀티스텝 (검색 → 요약 → 파일 저장)
- 프롬프트 인젝션·경로 우회 회귀 테스트 통과

**v1 — Phase 7~8 필수**

- STT + TTS
- `--voice`에서만 동작하는 로컬 웨이크워드 “자비스”
- 트레이/전역 단축키 UI
- 승인 요청·작업 완료 알림
- 설치/재시작 후에도 설정·기억·복구가 정상 동작

**Could (이후)**

- 로컬 7B~14B 오프라인 모드
- 캘린더/메일 연동
- 피드백 통계로 규칙 자동 조정

### 2.2 비목표 (v1에서 하지 않음)

- 풀로컬 70B급 모델로 모든 지식 대체
- 화면 비전으로 GUI를 완전 자율 조작
- 사용자 피드백으로 모델 가중치 파인튜닝/RLHF
- 삭제·포맷·결제·계정 변경의 자동 실행
- 처음부터 복잡한 멀티 에이전트 프레임워크

### 2.3 릴리스 경계와 종료 조건

| 릴리스 | 포함 Phase | 사용자에게 보이는 결과 | 출시 조건 |
|--------|------------|------------------------|-----------|
| MVP | 0~3 | 기억하고 근거를 찾아주는 CLI 비서 | 검색·기억·개인정보 테스트 통과 |
| v0.5 | 4~6 | 안전하게 PC 작업을 수행하는 텍스트 에이전트 | Critical/High 보안 결함 0건 |
| v1 | 7~8 | 음성·단축키로 호출하는 상주형 비서 | 1주 안정화 테스트와 최종 데모 통과 |

Phase를 완료했다는 의미는 기능이 한 번 동작했다는 뜻이 아니라, 해당 Phase의 **자동 테스트·실패 테스트·완료 기준**을 모두 통과했다는 뜻이다.

---

## 3. 하드웨어·현실적 한계

### 3.1 RTX 4070 Ti 12GB로 가능한 범위

| 작업 | 가능 여부 | 메모 |
|------|-----------|------|
| STT (Whisper) | 충분 | medium 이하부터 권장 |
| TTS | 충분 | |
| PC 조종 | 충분 | GPU 거의 불필요 |
| 로컬 LLM 7B~9B | 쾌적 | |
| 로컬 LLM 13B~14B (양자화) | 실사용 가능 | |
| 로컬 LLM 32B | 가능하나 빡셈 | 문맥·속도 희생 |
| 로컬 70B+ | 비현실 | |
| Ollama 9B + 로컬 도구 | **선택됨** | D005, `qwen3.5:9b` Q4_K_M |

### 3.2 저장 용량 가이드

| 용도 | 권장 여유 |
|------|-----------|
| 대화 기억(raw/요약/facts/인덱스) | 초기 quota 5GB, 80%에서 정리 경고 |
| 문서 RAG 원본 | 문서량에 비례 (원문의 2~3배 여유) |
| STT/TTS 모델 | 수 GB |
| 로컬 LLM (선택) | 모델당 4~10GB+ → 총 50~100GB 여유 권장 |

**기억만** 쌓는다면 용량 부담은 작다. 커지는 쪽은 주로 **로컬 모델 파일**이다.

---

## 4. 전체 아키텍처

```
[키보드 / 마이크]
        │
        ▼
┌───────────────────┐
│  Input Layer      │  텍스트 입력 / STT(후반)
└─────────┬─────────┘
          ▼
┌───────────────────┐
│  Orchestrator     │  의도 파악 → 계획 → 도구 호출 → 관찰 → 반복
│  (Agent Loop)     │
└─────────┬─────────┘
          │
    ┌─────┼──────────┬──────────┬────────────┐
    ▼     ▼          ▼          ▼            ▼
 Memory  LLM Brain  Tools   Privacy Gate  Safety Gate
 (D:)    (API 주)   (로컬)  (전송/마스킹)  (승인/권한)
    │     │          │          │            │
    └─────┴──────────┴──────────┴────────────┘
          ▼
┌───────────────────┐
│  Output Layer     │  화면 텍스트 / TTS(후반)
└───────────────────┘
          ▼
   Checkpoint / Session End → Summarizer → candidate memory
```

### 4.1 계층별 책임

| 계층 | 책임 | 실행 위치 |
|------|------|-----------|
| Input | 사용자 입력 수집 | 로컬 |
| Orchestrator | 도구 선택·루프·한도 관리 | 로컬 |
| LLM Brain | 이해·계획·요약·말투 | API (선택: 로컬) |
| Memory | 저장·검색·세션 요약 | D:\Jarvis\memory |
| Tools | 실제 행동 | 로컬 |
| Privacy | 외부 전송 허용·마스킹·보존 정책 | 로컬 |
| Safety | 허용/거부/승인 | 로컬 |
| Output | 응답 표시·음성 | 로컬 |

### 4.2 런타임에 한 번 질문이 처리되는 순서

1. 사용자 입력을 받고 `request_id`를 발급한다.
2. 사용자 입력 이벤트를 raw 로그에 **먼저 append + flush**한다.
3. Memory에서 관련 facts / corrections / summaries를 검색한다.
4. Privacy Gate가 API 전송 대상에서 시크릿·민감정보를 제거한다. 정책상 전송할 수 없으면 로컬 처리하거나 사용자에게 승인을 요청한다.
5. LLM에 시스템 지시, 허용된 기억, 현재 대화, 도구 스키마를 전달한다.
6. LLM이 최종 답변 또는 구조화된 `tool_call`을 제안한다.
7. Safety Gate가 도구명과 **정규화된 전체 인자**를 검증한다. 승인이 필요하면 실행될 정확한 대상·경로·변경 내용을 보여주고 승인받는다.
8. 도구 실행 의도를 감사 로그에 flush한 뒤 제한 시간 안에서 실행한다.
9. 성공/실패/변경 결과를 감사 로그에 기록하고 LLM에 전달한다.
10. 최종 답변을 표시하고 raw 로그에 저장한다. TTS는 민감정보 낭독 정책을 통과한 경우에만 실행한다.
11. 일정 턴마다 체크포인트를 만들고, 정상 종료 시 Summarizer가 memory 후보 레코드를 생성한다.
12. 다음 실행에서 미완료 세션이 발견되면 raw 로그로 복구하거나 사용자에게 폐기 여부를 묻는다.

---

## 5. 저장 구조 (D 드라이브)

소스 저장소는 `D:\Ai\Jarvis`, **운영 데이터·기억은 `D:\Jarvis`**로 분리한다. 운영 데이터 트리는 아래를 따른다.

```
D:\Jarvis\
├── config\
│   ├── settings.yaml        # 경로, 모델, 한도
│   ├── tools.yaml           # 허용 도구·인자·권한 화이트리스트
│   └── privacy.yaml         # 외부 전송, 보존, 마스킹 정책
├── memory\
│   ├── raw\                 # 대화 원문 (날짜/세션별)
│   ├── export\              # 기억 내보내기 결과
│   └── jarvis.sqlite3       # summaries/facts/corrections/task/검색 인덱스
├── docs\                    # RAG용 사용자 문서
├── skills\                  # 사용자 정의 스크립트/도구
├── state\                   # 실행 중 task, checkpoint, 복구 상태
├── logs\                    # 실행·도구·오류 로그
├── models\                  # (선택) Whisper, 로컬 LLM
└── backups\                 # 암호화 백업 staging (실제 사본은 별도 장치/위치)
```

개발용 `.env`가 필요하면 `D:\Ai\Jarvis\.env`에 두고 `.gitignore`에 등록한다. 운영 실행에서는 `.env` 대신 OS 보안 저장소를 사용한다.

### 5.1 저장 역할

| 저장소 | 내용 | 검색 우선순위 |
|--------|------|----------------|
| SQLite `kind=correction` | 사용자가 고친 정보 | 같은 key/주제 안에서 최우선 |
| SQLite `kind=fact` | 이름, 선호, 확정 사실 | 높음 |
| SQLite `kind=summary` | 세션 요약 | 중간 |
| `memory\raw\*.jsonl` | 원문 대화 | 낮음(복구·요약 시) |
| SQLite `documents/doc_chunks` | `docs\` 참고 문서 인덱스 | 질문 유형에 따라 |

### 5.2 세션 종료 시 저장 개념 (예시 필드)

```json
{
  "session_id": "ses_01J8Z9J0000000000000000",
  "summary": "사용자가 자비스 기억 구조와 검색 요약 방식을 논의함",
  "fact_candidates": [
    {"id": "fact_01J8Z9P00000000000000", "key": "pref.answer_style", "value": "짧고 근거 포함", "status": "candidate", "source_turn_id": "turn_01J8Z9K10000000000000"}
  ],
  "corrections": [],
  "tags": ["jarvis", "memory", "rag"],
  "raw_path": "memory/raw/ses_01J8Z9J0000000000000000.jsonl"
}
```

### 5.3 기억 레코드 스키마

Summarizer가 만든 사실은 바로 확정 기억이 아니라 `candidate`로 저장한다. 사용자가 `기억해:`로 직접 입력했거나 후보를 승인한 경우에만 `confirmed`가 된다.

```json
{
  "id": "fact_01J8Z9P00000000000000",
  "key": "pref.answer_style",
  "value": "짧고 근거 포함",
  "status": "confirmed",
  "source": {"session_id": "ses_01J8Z9J0000000000000000", "turn_id": "turn_01J8Z9K10000000000000", "kind": "user_explicit"},
  "created_at": "2026-08-10T20:30:00+09:00",
  "updated_at": "2026-08-10T20:30:00+09:00",
  "expires_at": null,
  "sensitivity": "normal",
  "supersedes": null,
  "tags": ["preference"]
}
```

필수 상태는 `candidate`, `confirmed`, `superseded`, `deleted`다. correction은 관련 없는 모든 fact보다 무조건 우선하지 않고, **같은 key/주제의 기존 레코드만** `superseded` 처리한다.

### 5.4 보존·삭제·복구 정책

- raw 로그: 기본 90일 보존 후 사용자 설정에 따라 삭제 또는 압축
- SQLite summaries/facts/corrections: 사용자가 목록 조회·수정·삭제·내보내기 가능
- 삭제: 본문·인덱스에 tombstone을 남기고, 백업은 보존 기간 만료 또는 재작성 시 함께 제거
- 쓰기: raw·설정은 임시 파일/append + flush, 상태 데이터는 처음부터 SQLite 트랜잭션 사용
- 복구: 시작 시 미완료 세션·부분 작성 파일을 탐지하고 자동 복구 결과를 로그에 남김
- 백업: `backups\`는 staging으로만 사용하고, 암호화된 사본을 별도 물리 장치 또는 별도 보안 위치에 보관하며 월 1회 복원 테스트 수행

---

## 6. 기술 스택

### 6.1 권장 기본안

| 영역 | 선택 | 이유 |
|------|------|------|
| 언어 | Python 3.11+ | AI·자동화 생태계 |
| LLM | Ollama 0.32.6 + `qwen3.5:9b` Q4_K_M | 한국어·로컬 처리·외부 호출 비용 0, D005 |
| LLM 접속 | `http://127.0.0.1:11434`만 허용 | 클라우드 폴백과 외부 전송 방지 |
| STT | faster-whisper | 4070 Ti와 잘 맞음 |
| TTS | edge-tts(온라인) 또는 로컬 TTS | 빠른 도입, 오프라인 요구 시 로컬 선택 |
| 기억 검색 | SQLite + FTS5(trigram), 이후 선택적 임베딩 | 트랜잭션·한국어 부분 검색·점진 강화 |
| 웹 검색 | 검색 API (예: Tavily) 또는 통제된 브라우저 도구 | URL 근거 |
| PC 제어 | `subprocess`, `os.startfile`, 제한적 GUI 자동화 | 안전 우선 |
| UI | CLI → 트레이/단축키 앱 | 검증 속도 |
| 설정 | YAML/JSON + OS 보안 저장소 (`.env`는 개발용) | 키·경로 분리 |

### 6.2 초반에 쓰지 않는 것

- 과도한 에이전트 프레임워크 (직접 함수 호출이 익숙해진 뒤)
- 여러 로컬 거대 모델 동시 상주
- 화면 비전 기반 완전 자율 클릭

---

## 7. 개발이 진행되는 전체 흐름

이 섹션은 **시간이 지날수록 제품이 어떻게 변하는지**를 설명한다.

### 7.1 큰 그림 (성숙 단계)

```
[레벨 0] 빈 프로젝트·설정
    ↓
[레벨 1] 말하는 챗봇 (저장만 함)
    ↓
[레벨 2] 기억하는 챗봇 (요약·facts)
    ↓
[레벨 3] 찾아주는 비서 (웹/문서 + 출처)
    ↓
[레벨 4] 손 있는 비서 (안전한 PC 조종)
    ↓
[레벨 5] 일 시키는 에이전트 (여러 도구 연결)
    ↓
[레벨 6] 말하는 자비스 (STT/TTS)
    ↓
[레벨 7] 상주형 비서 (트레이·단축키·알림)
```

### 7.2 사용자 입장에서 체감 변화

| 시점 | 사용자가 느끼는 변화 |
|------|----------------------|
| Phase 1 후 | “챗GPT처럼 대화되고, 기록이 파일로 남는다” |
| Phase 2 후 | “저번에 말한 선호를 다음에도 안다” |
| Phase 3 후 | “검색해서 링크와 함께 정리해 준다” |
| Phase 4 후 | “앱·폴더를 대신 열어 준다” |
| Phase 5 후 | “찾아 → 정리 → 저장을 한 번에 한다” |
| Phase 7 후 | “말로 시켜도 된다” |

### 7.3 개발자 입장에서 진행 방식

1. **수직 슬라이스:** 매 Phase마다 “작은 데모가 돌아가게” 만든다.
2. **게이트:** Phase 완료 기준을 통과하기 전에 다음 Phase로 넘어가지 않는다.
3. **회귀 테스트:** 12장의 고정 시나리오 중 현재 Phase까지 적용되는 항목을 매번 다시 돌린다.
4. **안전 기본값:** 새 도구는 기본 deny, 화이트리스트에 넣을 때만 allow.
5. **기억 품질:** 요약 자동화와 별도로, 중요 facts는 사용자 확정을 선호한다.

### 7.4 공부 로드맵과의 진행 동기화

| 공부 순서 | 개발 Phase | 연결 포인트 |
|-----------|------------|-------------|
| 1~3. 통계/뉴럴/트랜스포머 | Phase 0 전·병행 | 이론 (왜 LLM이 이렇게 동작하는지) |
| 4. 대화형 AI | Phase 1 | 프롬프트, 멀티턴, 환각 |
| 5. RAG | Phase 2~3 | 기억 검색, 웹/문서 근거 |
| 6. 추론·RL 개념 | Phase 5 전후 | 계획·도구 선택 이해 (필수 구현은 아님) |
| 7. AI 에이전트 | Phase 4~5 | 도구 호출 루프 |
| (응용) 음성 | Phase 7 | STT/TTS |

**추천:** 이론 1~3은 가볍게 병행하고, **학습 항목 4→5→7 순으로 구현 코드와 함께** 간다.

---

## 8. Phase별 상세 계획

각 Phase는 **목표 → 작업 → 산출물 → 완료 기준 → 다음으로 넘어가는 조건**으로 구성한다.

---

### Phase 0 — 기반 세팅

**목표:** 실행 가능한 빈 골격

**작업**

- 경로 역할 고정
  - 소스 저장소: `D:\Ai\Jarvis`
  - 운영 데이터: `D:\Jarvis`
- Python venv, 의존성 관리 (`requirements.txt` 또는 `pyproject.toml`)
- API 키 로드: 운영은 Windows Credential Manager/DPAPI 등 OS 보안 저장소 우선, `.env`는 개발용으로만 사용
- `config/settings.yaml` (memory 경로 = `D:\Jarvis\memory`)
- `config/privacy.yaml` (외부 전송 허용 범위, 마스킹, 보존 기간)
- `config/tools.yaml` 정책 로더와 기본 deny 골격
- CLI 엔트리포인트 (`python -m app`)
- 이벤트 로그 스키마 (`request_id`, `session_id`, `event_type`, timestamp)와 append + flush
- 원자적 파일 쓰기 유틸리티와 비정상 종료 복구 골격
- 로그 시크릿 마스킹 필터

**산출물**

- 실행하면 프롬프트가 뜨는 CLI
- 설정으로 경로가 바뀌는 구조
- 테스트용 임시 데이터 경로를 사용해 운영 기억을 건드리지 않는 구조

**완료 기준**

- [ ] 가상환경에서 앱이 실행된다
- [ ] API 키를 코드에 하드코딩하지 않는다
- [ ] 로그 파일이 생성된다
- [ ] 기본 도구 정책이 deny이며 미등록 도구는 실행되지 않는다
- [ ] 테스트 로그에 API 키·비밀번호 패턴이 평문으로 남지 않는다
- [ ] 강제 종료 후 부분 작성 파일을 탐지하고 복구 또는 격리한다

**예상 기간:** 2~4일

---

### Phase 1 — 대화형 AI (챗봇)

**목표:** 로컬 Ollama로 대화하고 raw를 저장한다

**작업**

- 시스템 프롬프트 정의 (한국어, 추측 최소화 등)
- 멀티턴 메시지 히스토리
- 사용자 입력은 모델 호출 전에, 응답은 수신 직후 `memory/raw/`에 이벤트 단위 append + flush (jsonl 권장)
- Ollama 연결 실패·timeout·5xx 처리, 지수 backoff, 요청 취소
- 요청별 모델·토큰·비용·지연시간 기록 (본문과 분리된 메타데이터)
- N턴마다 세션 체크포인트 생성, 시작 시 미완료 세션 복구
- 간단한 명령: `/help`, `/clear`, `/bye`

**산출물**

- 동작하는 챗봇
- 날짜/세션별 raw 로그

**완료 기준**

- [ ] 연속 대화가 문맥을 유지한다
- [ ] 종료 후 raw 파일이 남아 있다
- [ ] Ollama 오류 시 사용자에게 메시지가 간다
- [ ] 응답 도중 강제 종료해도 이미 입력한 사용자 메시지가 유실되지 않는다
- [ ] 연결 실패/timeout/5xx를 재현한 테스트가 무한 재시도 없이 종료된다
- [ ] loopback 외 외부 HTTP 호출이 0건이다

**데모**

- 10분 대화 → `raw` 파일 열어 확인

**예상 기간:** 약 1주  
**학습 연결:** 대화형 AI

---

### Phase 2 — 기억 (세션 요약 + 다음 세션 활용)

**목표:** 컨텍스트가 끝나도 “나에 대한 정보”가 남는다

**작업**

- 세션 종료(`/bye`) 시 Summarizer 호출
- SQLite `records.kind`로 summary/fact/correction을 구분해 트랜잭션 저장
- Summarizer 결과는 `candidate`로 저장하고 자동 생성 confidence만으로 확정하지 않음
- `기억해:`는 `confirmed`, `틀림:`은 같은 key/주제의 기존 기억을 `superseded` 처리
- 새 세션 시작 시 관련 기억 top-k를 시스템 컨텍스트에 삽입
- 사용자 피드백 명령
  - `기억해: ...`
  - `틀림: (잘못된 내용) → (올바른 내용)`
- 기억 관리 명령: `/memory list`, `/memory confirm`, `/memory edit`, `/forget`, `/memory export`
- 정상 종료가 아니어도 마지막 체크포인트까지 요약 후보를 재생성할 수 있게 함
- (초기) SQLite FTS5 trigram 검색 → 이후 선택적 임베딩

**진행 중 동작 흐름**

```
대화 중: raw에 계속 append
    ↓
/bye
    ↓
Summarizer(LLM) → summary + candidate facts
    ↓
명시적 `기억해:` 또는 사용자 승인 → confirmed
    ↓
다음 실행
    ↓
질문 key/FTS (이후 임베딩) → 관련 기억 검색 → 프롬프트에 포함 → 답변
```

**완료 기준**

- [ ] 종료 후 SQLite에 summary 레코드가 생긴다
- [ ] 재실행 후 “내 선호”를 묻지 않고도 반영한다
- [ ] correction이 같은 key의 이전 기억을 superseded 처리한다
- [ ] candidate 기억은 승인 전 일반 답변에 확정 사실처럼 사용되지 않는다
- [ ] `/forget` 후 해당 기억이 검색 인덱스에서도 제외된다

**주의**

- 요약이 틀리면 잘못된 기억이 고착될 수 있음
- 비밀번호·토큰·민감정보는 저장 금지 목록과 패턴 필터로 제외

**예상 기간:** 1~2주  
**학습 연결:** RAG 입문 (검색·컨텍스트 주입)

---

### Phase 3 — 웹/문서 검색 → 요약 전달

**목표:** “모델 머릿속”이 아니라 “찾아서” 답한다

**작업**

- Web Search 도구 1개 연동
- 웹/문서 본문은 모두 **신뢰할 수 없는 데이터**로 표시하고 그 안의 명령문은 실행 지시로 취급하지 않음
- 외부 콘텐츠가 시스템 프롬프트·사용자 의도·도구 권한을 변경할 수 없도록 Orchestrator에서 신뢰 경계 강제
- 응답 포맷 고정
  1. 핵심 요약
  2. 주장별 근거 링크와 확인 날짜 (필수)
  3. 상태: `확실` / `상충` / `근거 부족`
- 가능하면 2개 이상 출처 교차
- `docs\` 로컬 RAG (청킹 → 검색 → 인용문 포함)
- “근거 문장에 없으면 단정하지 않기” 규칙
- Web fetcher는 `http/https`만 허용하고 redirect 횟수·본문 크기·timeout 제한; loopback, 사설 IP, `file:` 및 사용자 정의 프로토콜 차단
- 문서별 `local_only`/`api_allowed` 전송 등급과 전송 전 민감정보 마스킹

**완료 기준**

- [ ] 검색 질문에 URL이 답변에 포함된다
- [ ] 검색 실패 시 솔직히 실패를 말한다
- [ ] 로컬 문서 질문 시 파일명/인용을 보여준다
- [ ] 악성 웹/문서의 “이전 지시 무시·도구 실행” 문구가 도구 호출로 이어지지 않는다
- [ ] `local_only` 문서 원문이 외부 API 요청에 포함되지 않는다
- [ ] 각 핵심 주장과 출처를 역추적할 수 있다

**예상 기간:** 1~2주  
**학습 연결:** RAG 시스템

---

### Phase 4 — PC 조종 v0.5 (안전한 손발)

**목표:** 허용된 범위에서 OS를 조작한다

**작업**

- `config/tools.yaml` 화이트리스트
  - 예: 메모장, 탐색기, Chrome, VS Code, 특정 폴더 열기
- 타입이 정해진 도구 스키마를 LLM에 제공하고 알 수 없는 필드·과도한 길이·제어문자를 거부
- Secure Tool Runner 구현
  - `shell=True`와 문자열 셸 명령 금지, 실행 파일과 인자를 배열로 전달
  - 경로를 canonical path로 변환하고 `..`, junction/symlink/reparse point를 거친 sandbox 우회 차단
  - timeout, 출력 크기, 동시 실행 수, 하위 프로세스 제한
  - 실행 전 정확한 도구명·전체 인자·예상 변경을 감사 로그에 flush
- 모든 도구 실행 결과와 승인 주체·시각·대상 인자 hash를 `logs\`에 기록
- 승인은 표시한 정확한 인자에만 유효하며 인자가 바뀌면 다시 승인
- 기본 거부: 삭제, 포맷, 임의 셸, 다운로드 실행, 레지스트리

**예시 도구**

| 도구 | 위험도 | 설명 |
|------|--------|------|
| `open_app` | Low | 허용 목록 앱 실행 |
| `open_folder` | Low | 허용 경로만 |
| `open_url` | Low/Medium | `http/https`만. 미등록 도메인·민감 쿼리는 확인 |
| `create_file` | Medium | 허용 경로에 새 파일 생성, 덮어쓰기는 별도 승인 |
| `run_skill` | High | v0.5 기본 비활성. 등록 hash·권한 manifest·명시적 승인 필요 |

**완료 기준**

- [ ] “메모장 열어줘”가 동작한다
- [ ] 허용되지 않은 앱 요청은 거절 + 이유를 말한다
- [ ] 실행 이력이 로그에 남는다
- [ ] path traversal, junction/symlink, 악성 URL scheme 테스트가 모두 차단된다
- [ ] 승인 화면과 실제 실행 인자가 다르면 실행되지 않는다
- [ ] timeout된 프로세스와 허용되지 않은 하위 프로세스가 남지 않는다

**예상 기간:** 1~2주  
**학습 연결:** AI 에이전트 (도구 사용)

---

### Phase 5 — 에이전트 루프 (일 시키기)

**목표:** 여러 도구를 순서대로 연결한다

**작업**

- Agent loop 구현
  - Decide(tool 또는 final) → Validate → Act → Observe → (repeat) → Final Answer
  - 내부 추론 원문은 저장하지 않고 사용자에게 보여줄 짧은 작업 계획·도구 호출·결과만 기록
- `max_steps` (예: 6~8)로 무한 루프 방지
- `task_id`와 단계별 상태(`pending/running/succeeded/failed`) 저장, 재시작 시 중복 실행 방지
- 재시도는 검색 등 안전하고 idempotent한 동작만 허용; 파일 쓰기·외부 변경은 자동 재시도 금지
- 웹/문서의 지시는 새 사용자 의도로 승격하지 않으며, 각 부작용 단계는 원래 사용자 요청과 다시 대조
- 복합 태스크 예
  - “주제 검색 → 요약 → `docs\notes\`에 저장 → 폴더 열기”
- 실패 시 완료된 단계·실패 단계·미실행 단계를 구분해 보고

**완료 기준**

- [ ] 단일 지시로 검색+저장이 이어진다
- [ ] step 한도 초과 시 안전하게 멈춘다
- [ ] 중간 실패가 로그와 사용자 메시지에 남는다
- [ ] 강제 종료 후 재개해도 완료된 쓰기 작업이 중복 실행되지 않는다
- [ ] 검색 결과에 포함된 명령이 사용자 승인 없이 새 작업을 만들지 않는다

**예상 기간:** 1~2주

---

### Phase 6 — 보안 강화 및 v0.5 릴리스 게이트

**목표:** 도움이 되면서도 사고를 막는다

**작업**

- Phase 4의 Secure Tool Runner를 대상으로 위협 모델·레드팀 테스트 수행
- 위험도 등급
  - **Low:** 자동 실행
  - **Medium:** `y/n` 확인
  - **High:** 승인 + 확인 문구 타이핑
- 쓰기 경로 sandbox (`D:\Jarvis` + 허용 폴더)
- 시크릿 필터 (API 키, 비밀번호 패턴)와 API/로그/TTS별 유출 테스트
- 도구별 capability manifest: 파일 읽기/쓰기, 네트워크, 프로세스 실행 권한을 별도 선언
- 감사 로그 무결성, 로그 rotation, 보존·삭제 정책
- 암호화 백업과 실제 복원 테스트
- (선택) 승인 UI를 CLI에서 작은 팝업으로

**완료 기준**

- [ ] High 위험 도구는 승인 없이 실행되지 않는다
- [ ] sandbox 밖 쓰기 시도가 차단된다
- [ ] prompt injection, path traversal, 악성 URL, 인자 삽입 테스트를 자동화한다
- [ ] 테스트 기준 Critical/High 보안 결함이 0건이다
- [ ] 백업에서 새 데이터 디렉터리로 복원하는 데 성공한다

**예상 기간:** 1~2주 (Phase 4~5와 일부 병행 가능하지만 릴리스 게이트는 마지막에 수행)

---

### Phase 7 — 음성 입출력

**목표:** 자비스의 “귀와 입”

**작업**

- Vosk 제한 문법으로 “자비스” 호출어를 감지하고 faster-whisper small로 한국어 질문을 로컬 인식
- `--voice` opt-in 실행에서 “자비스” 웨이크워드, PTT는 후속 보조 입력
- Windows SAPI 한국어 로컬 TTS로 짧은 응답 낭독 (긴 요약은 화면 우선)
- Heami 약 1.5배 읽기와 답변 중 “자비스” 호출에 의한 즉시 중단·새 질문 전환
- 기존 Orchestrator는 그대로 재사용 (입력만 음성으로)

**완료 기준**

- [ ] 말로 Phase 4~5와 동일한 작업을 수행할 수 있다
- [ ] 인식 실패 시 재요청 안내
- [ ] 승인 대상의 앱·경로·변경 내용을 화면에 표시하고 음성만으로 High 작업을 승인하지 않는다

**예상 기간:** 1~2주  
**GPU:** 4070 Ti로 STT는 여유. 로컬 LLM과 동시라면 VRAM 조절 필요.

---

### Phase 8 — 상주 UX

**목표:** 터미널을 매번 켜지 않아도 된다

**작업**

- 시스템 트레이 아이콘
- 전역 단축키로 호출
- 상태 표시: Listening / Thinking / Waiting approval
- 작업 완료·승인 요청 알림
- 단일 인스턴스 lock, 정상 종료 메뉴, 자동 시작은 사용자 opt-in
- Windows 재시작 후 미완료 task 복구 안내
- 패키징된 실행 파일/설치 스크립트와 설정·데이터 경로 migration 테스트

**완료 기준**

- [ ] 단축키로 입력창이 뜬다
- [ ] 백그라운드에서 대기 가능하다
- [ ] 마이크 사용 중임을 항상 시각적으로 표시한다
- [ ] 두 번째 인스턴스 실행과 중복 전역 단축키 등록을 차단한다
- [ ] Windows 재시작 후 데이터 손상 없이 미완료 task 상태를 보여준다

**예상 기간:** 약 1주

---

### Phase 9 — 선택 고도화

- 로컬 7B~14B로 간단 작업·오프라인 모드
- 임베딩 검색 고도화, fact 중복 병합 UI
- 외부 서비스 연동 (캘린더 등, 읽기 우선)
- 피드백 통계로 검색 우선순위·프롬프트 미세조정
- 웨이크워드 오탐률 고도화와 사용자별 호출어
- (훨씬 나중에) 데이터 충분 시 파인튜닝 검토

---

## 9. 기억·피드백·성장 방식

### 9.1 “성장”의 의미

이 프로젝트에서 성장은 **모델 가중치가 커지는 것**이 아니라,

- 나에 대한 **기억(facts)** 이 늘고
- **교정(corrections)** 이 쌓이며
- 검색·규칙이 나아져

**다음 유사 질문에 더 맞게 답하는 것**을 의미한다.

### 9.2 피드백이 저장되는 경로

| 사용자 행동 | 저장 위치 | 다음 효과 |
|-------------|-----------|-----------|
| 일반 대화 | raw JSONL → 종료 시 SQLite summary | 맥락 유지 |
| `기억해: ...` | SQLite fact | 선호/사실 반영 |
| `틀림: A → B` | SQLite correction | 오답 재발 감소 |
| 좋아요/싫어요(선택) | 로그/통계 | 규칙·프롬프트 조정 재료 |

### 9.3 되지 않는 것

- API 제공 모델 자체가 당신 피드백으로 재학습되지 않음
- 저장만 하고 검색하지 않으면 “기억한 척”만 하고 다음엔 못 씀

### 9.4 잘 되게 하는 규칙

1. 먼저 질문과의 관련성을 계산하고, **같은 key/주제 안에서만** correction > confirmed fact > summary > raw 순으로 우선한다.
2. `candidate`는 확정 사실처럼 답변에 사용하지 않고, 중요 사실은 **사용자 확정**을 거친다.
3. 모든 기억은 출처 세션·턴과 생성/수정 시각을 보존한다.
4. 민감정보는 저장·API 전송·로그·TTS 각각에서 독립적으로 차단한다.
5. 새 correction은 이전 레코드를 삭제하지 않고 `superseded`로 연결해 변경 이력을 남긴다.
6. 사용자는 기억을 언제든 조회·수정·삭제·내보낼 수 있다.

---

## 10. 정보 신뢰성 (검색·요약 검증)

완전 자동 “진실 판별기”는 없다. 아래 절차로 **신뢰 가능한 전달**을 만든다.

1. **출처 동봉:** 요약 + URL/문서명/인용
2. **교차 확인:** 가능하면 독립된 2개 이상 출처
3. **원문 대조:** 검색 결과에 없는 내용은 단정 금지
4. **최신성:** 날짜가 중요한 주제는 작성 시점 표시
5. **상태 라벨:** 확실 / 상충 / 근거 부족
6. **고위험 주제:** 의료·법률·금융·보안은 1차 출처 직접 확인 권고
7. **중요 행동 전 승인:** 검색 결과를 바탕으로 한 시스템 변경은 항상 확인
8. **신뢰 경계:** 웹·메일·문서·도구 출력에 포함된 명령은 사용자 또는 시스템 지시로 취급하지 않음
9. **주장-근거 연결:** 핵심 주장마다 실제로 그 주장을 뒷받침하는 출처와 확인 날짜를 연결
10. **도구 권한 분리:** 검색 결과는 도구 호출의 인자로 자동 승격하지 않고 원래 사용자 의도와 정책을 다시 검증

---

## 11. 안전·권한 설계

### 11.1 기본 원칙

- 기본 거부, 명시적 허용만 실행
- 설명 가능한 로그
- 위험도에 따른 인간 승인
- 쓰기 경로 제한
- 웹/문서/도구 출력은 권한 없는 데이터로 취급
- 승인은 도구명·정규화된 인자·대상·예상 변경에 결합하고 변경되면 무효화
- 승인 UI에는 실행 직전 값만 표시하며, LLM이 만든 설명만 믿지 않고 정책 엔진이 계산한 대상을 표시

### 11.2 위험 등급 예시

| 등급 | 예 | 정책 |
|------|----|------|
| Low | 허용 앱 실행, 폴더 열기 | 자동 |
| Medium | 허용 경로의 새 파일 생성, 미등록 도메인 열기 | 정확한 대상 표시 후 확인 |
| High | 등록 skill 실행, 덮어쓰기, 설치, 시스템 설정 | 확인 문구 입력 + v0.5 기본 비활성 가능 |
| Forbidden | 삭제·포맷·임의 셸·보안 설정 해제 | v1에서는 승인 여부와 무관하게 차단 |

### 11.3 실행 금지 (v1)

- 임의 파일/디렉터리·디스크 삭제 및 포맷 (`/forget`은 memory 전용 안전 삭제 경로로 별도 구현)
- 비밀번호·결제·계정 권한 변경
- 출처 불명 프로그램 다운로드 후 실행
- 사용자 홈 전체·시스템 디렉터리 무단 탐색·수정

---

## 12. 테스트·데모 시나리오

매 Phase 종료 시 현재까지 구현된 항목을 자동 회귀 테스트한다. 테스트에서는 운영 데이터가 아닌 임시 디렉터리와 가짜 API를 사용한다.

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | 이름/선호 저장 후 재실행 | 기억 반영 |
| 2 | 모르는 최신 사실 | 추측하지 않거나 검색 |
| 3 | 웹 요약 요청 | 요약 + URL |
| 4 | 로컬 문서 질문 | 인용/파일명 |
| 5 | 허용 앱 실행 | 성공 |
| 6 | 비허용 앱 실행 | 거절 + 이유 |
| 7 | 임의 파일 삭제 요청 | 거부 |
| 8 | 과거 틀린 답 correction | 교정 우선 |
| 9 | 복합: 검색→저장 | 멀티스텝 성공 |
| 10 | (후반) 음성 동일 지시 | 텍스트와 동일 결과 |
| 11 | 응답 중 프로세스 강제 종료 | 입력 로그 보존 + 다음 시작 시 복구 |
| 12 | candidate 기억을 사실처럼 질문 | 확정 사실로 단정하지 않음 |
| 13 | `/forget` 후 같은 기억 검색 | 본문·인덱스 모두 검색 제외 |
| 14 | `local_only` 문서 질문 | 문서 원문을 외부 API로 전송하지 않음 |
| 15 | 악성 문서의 “지시 무시·도구 실행” | 데이터로만 요약하고 도구 실행 안 함 |
| 16 | `..`, junction/symlink로 sandbox 우회 | 차단 + 감사 로그 |
| 17 | `file:`, `javascript:`, 사용자 정의 URL scheme | 차단 |
| 18 | 승인 후 인자 바꿔치기 | 기존 승인 무효 + 재승인 |
| 19 | 쓰기 완료 직후 재시작·재개 | 같은 파일 작업 중복 실행 없음 |
| 20 | 암호화 백업 복원 | 새 데이터 경로에서 기억 검색 성공 |

### 12.1 릴리스별 정량 기준

| 항목 | MVP | v0.5 | v1 |
|------|-----|------|----|
| 고정 기억 질의 정확도 | confirmed 테스트셋 90% 이상 | 95% 이상 | 95% 이상 |
| 근거 제공 | 검색형 핵심 주장 100%에 URL/문서명 | 동일 | 동일 |
| 금지 동작 오실행 | 0건 | 0건 | 0건 |
| 비정상 종료 복구 | 20회 테스트에서 입력 유실 0건 | 중복 부작용 0건 | 동일 |
| Critical/High 보안 결함 | Critical 0건 | Critical/High 0건 | Critical/High 0건 |
| 비용 한도 | 일/월 예산 설정, 80% 경고·100% 신규 요청 중지 | 동일 | 동일 |
| 지연시간 목표 | 기준 장비에서 일반 대화 p95 15초 이내 | Low 도구 시작 p95 3초 이내 | PTT 종료 후 STT 결과 p95 5초 이내 |
| 안정화 | 핵심 데모 20회 연속 성공 | 50개 복합 태스크 95% 이상 | 7일 사용 중 데이터 손상 0건 |

지연시간과 기억 정확도 수치는 Phase 1~2에서 기준값을 측정한 뒤 한 번 조정할 수 있지만, 조정 이유와 새 기준은 문서 이력에 남긴다.

### v1 최종 데모 스크립트

1. 단축키로 자비스 호출
2. “내 답변은 짧게, 출처 꼭” 기억시키기
3. 주제 하나 웹 검색 요약
4. 요약을 `D:\Jarvis\docs\notes\`에 저장
5. 해당 폴더 열기
6. 프로그램 재시작 후 말투/선호가 유지되는지 확인
7. 음성으로 “메모장 열어줘”를 실행
8. 악성 문서의 도구 실행 지시가 무시되는지 확인
9. 임의 파일 삭제 요청과 음성 High 승인 시도가 모두 차단되는지 확인

---

## 13. 일정·학습 로드맵 연결

### 13.1 파트타임 일정안 (하루 1~2시간)

| 주차 | Phase | 상태 목표 |
|------|-------|-----------|
| 1 | 0~1 | 챗봇 + raw 저장 |
| 2~3 | 2 | 기억 동작 |
| 4~5 | 3 | 검색 요약 |
| 6~7 | 4 | PC 조종 |
| 8~9 | 5~6 | 에이전트 + 안전 |
| 10~11 | 7 | 음성 |
| 12 | 8 | 상주 UX + 데모 |
| 13 | 전체 | 보안 재검증·장애 복구·패키징·7일 안정화 시작 |

→ 약 **3~3.5개월**에 v1 후보 빌드 가능. 이후 7일 안정화 기준을 통과하면 v1로 확정한다.

### 13.2 마일스톤

| 마일스톤 | 의미 |
|----------|------|
| M1 | 대화 + 저장 |
| M2 | 기억되는 비서 |
| M3 | 찾아주는 비서 |
| M4 | 손 있는 비서 |
| M5 | 안전하게 일 시키는 에이전트 (v0.5) |
| M6 | 음성·상주형 자비스 (v1 후보) |
| M7 | 7일 안정화 통과 (v1) |

---

## 14. 리스크와 대응

| 리스크 | 증상 | 대응 |
|--------|------|------|
| 환각 | 없는 사실 단정 | 출처 강제, 없으면 보류 |
| 잘못된 기억 | 틀린 fact 반복 | 같은 key의 correction 우선, candidate 확인 절차 |
| 도구 오남용 | 원치 않은 실행 | 화이트리스트, 승인 |
| 범위 폭발 | 아무것도 완성 안 됨 | Phase 게이트 엄수 |
| API 비용 | 청구 증가 | 컨텍스트 축소, 간단 분류 로컬화 |
| VRAM 경쟁 | STT+로컬LLM 동시 실패 | 동시 상주 제한, 모델 크기 축소 |
| 개인정보 유출 | API·로그·TTS·백업으로 비밀 노출 | 전송별 필터, OS 보안 저장소, 백업 암호화 |
| 외부 데이터 프롬프트 인젝션 | 문서가 도구 실행을 유도 | 신뢰 경계, 사용자 의도 재검증, 악성 문서 테스트 |
| API로 로컬 정보 유출 | 기억·문서 원문이 외부 전송 | 문서 전송 등급, 마스킹, 전송 차단/승인 |
| 부분 기록·중복 실행 | 강제 종료 후 기억 손상 또는 파일 중복 생성 | append+flush, 원자적 쓰기, task id, idempotency |
| 의존성 공급망 | 악성/취약 패키지 설치 | lockfile, hash 고정, 최소 의존성, 정기 업데이트 검토 |

---

## 15. 의사결정 체크리스트

초반 기본값을 정해 두고, 나중에 바꿔도 된다.

| 항목 | 기본 권장 | 내 선택 |
|------|-----------|---------|
| LLM | Ollama + `qwen3.5:9b` 하나만 고정 | ☑ |
| UI | CLI → 트레이 | ☐ |
| 음성 호출 | `--voice` opt-in 로컬 웨이크워드 “자비스” | ☑ |
| 기억 경로 | `D:\Jarvis\memory` | ☐ |
| 로컬 LLM | Phase 1부터 사용, 클라우드 폴백 없음 | ☑ |
| 웹 검색 제공자 | Phase 3에서 검색 API 1개만 선택 | ☐ |
| 운영 시크릿 저장 | Windows 보안 저장소 | ☐ |
| API 전송 기본값 | 명시적으로 허용된 기억·문서만 | ☐ |
| raw 로그 보존 | 기본 90일 | ☐ |
| 허용 쓰기 경로 | `D:\Jarvis\docs`, `D:\Jarvis\memory` 등 명시 | ☐ |
| 비용 예산 | 일/월 한도와 80% 경고 | ☐ |
| 백업 | 암호화 위치·주기·복원 책임 확정 | ☐ |

---

## 16. 바로 시작할 첫 작업

**이번 주 목표 = Phase 0 완료 + Phase 1 핵심 흐름**

1. `D:\Ai\Jarvis`를 소스 저장소, `D:\Jarvis`를 운영 데이터로 고정하고 폴더 트리 생성
2. Python 프로젝트 초기화, venv, lockfile, 테스트 러너 구성
3. `settings.yaml`, `privacy.yaml`, 기본 deny `tools.yaml` 작성
4. 개발용 시크릿 로더와 로그 마스킹 테스트 작성
5. `request_id` 기반 JSONL 이벤트 로그와 append + flush 구현
6. API 연결 CLI 챗봇에 timeout·취소·비용 메타데이터 추가
7. 프로세스 강제 종료 후 세션 복구 테스트 20회 수행
8. Phase 2 사전 검증으로 `/bye` 요약을 `candidate`로 1회 저장하고 다음 실행에서 사용자에게 확인 요청

이 8개가 끝나면 **안전하게 저장→복구→후보 요약**까지의 핵심 기반이 검증된다. PC 제어 코드는 Phase 4의 Secure Tool Runner와 승인 정책이 준비되기 전에는 추가하지 않는다.

---

## 부록 A. 용어

| 용어 | 의미 |
|------|------|
| RAG | 검색으로 자료를 가져와 생성에 넣는 방식 |
| Agent | 목표를 위해 도구를 반복 호출하는 루프 |
| STT | Speech-to-Text (음성→텍스트) |
| TTS | Text-to-Speech (텍스트→음성) |
| Fact | 다음에 재사용할 확정 정보 |
| Correction | 잘못된 응답을 고친 기록 |
| Whitelist | 허용된 도구/앱/경로만 실행하는 목록 |

## 부록 B. 관련 학습 로드맵 (참고)

1. 통계적 언어 모델  
2. 뉴럴 언어 모델  
3. 트랜스포머  
4. 대화형 AI ← 개발 Phase 1  
5. RAG 시스템 ← 개발 Phase 2~3  
6. 추론 모델과 강화학습 (개념)  
7. AI 에이전트 ← 개발 Phase 4~5  

---

## 문서 이력

| 날짜 | 내용 |
|------|------|
| 2026-08-10 | 초안 작성 (하이브리드 자비스 v1 플랜) |
| 2026-08-10 | v0.2: 릴리스 경계, Privacy/Safety Gate, 기억 lifecycle, 장애 복구, 보안·정량 테스트 보강 |
| 2026-08-10 | v0.3: README 개발 허브·Phase별 실행 문서 추가, SQLite 저장 구조로 기준 통일 |
| 2026-08-11 | v0.4: Vosk·Windows SAPI 기반 opt-in “자비스” 음성 세로 기능 반영 |
| 2026-08-11 | v0.5: Vosk 호출어 + faster-whisper small 질문 인식 하이브리드 STT 결정 반영 |

---

**다음 액션:** Phase 1 완료 증거를 유지한 채 [Phase 2 구현서](./docs/phase-02-memory/README.md)의 기억 lifecycle을 순서대로 착수한다.
