# Ollama + qwen3.5:9b does not require an LLM API key.
# Future external services may use the mappings below after their decisions.
#
# Credential Manager username -> development .env key mapping:
# search_api_key    -> JARVIS_SEARCH_API_KEY=
# backup_passphrase -> JARVIS_BACKUP_PASSPHRASE=
